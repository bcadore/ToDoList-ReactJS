import BarraLateral from '../../containers/BarraLateral';
import Formulario from '../../containers/Formulario';

const Cadastro = () => (
  <>
    <BarraLateral exibirFiltros={false} />
    <Formulario />
  </>
);

export default Cadastro;
